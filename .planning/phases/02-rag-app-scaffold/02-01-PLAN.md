---
phase: 02-rag-app-scaffold
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tailwind.config.ts
  - next.config.ts
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - components.json
autonomous: true
requirements:
  - RAGG-01
must_haves:
  truths:
    - "package.json exists with next, react, typescript, tailwindcss, @neondatabase/serverless, openai dependencies"
    - "tsconfig.json exists with App Router compatible config"
    - "npm run dev starts the Next.js dev server without error"
    - "components.json exists (shadcn configuration)"
  artifacts:
    - path: "package.json"
      provides: "Dependency manifest with all required packages"
    - path: "tsconfig.json"
      provides: "TypeScript configuration"
    - path: "components.json"
      provides: "shadcn/ui configuration file"
    - path: "src/app/globals.css"
      provides: "Base Tailwind + shadcn CSS variables"
  key_links:
    - from: "package.json"
      to: "@neondatabase/serverless"
      via: "npm install"
      pattern: "@neondatabase/serverless"
    - from: "package.json"
      to: "openai"
      via: "npm install"
      pattern: "\"openai\""
---

<objective>
Initialize the Next.js 14 App Router project in the existing repo root, install all required dependencies, and run shadcn init to create the components.json configuration. This establishes the package.json, tsconfig.json, and base globals.css that all subsequent tasks depend on.

Purpose: Wave 1 prerequisite — nothing else can run until the scaffold exists.
Output: Runnable Next.js 14 app with @neondatabase/serverless and openai installed; shadcn initialized; dev server starts without error.
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-rag-app-scaffold/02-CONTEXT.md
@.planning/phases/02-rag-app-scaffold/02-RESEARCH.md

<interfaces>
<!-- Existing project root before scaffold (confirmed via ls) -->
<!-- Files already present: CLAUDE.md, DESIGN.md, docs/ -->
<!-- These are NOT Next.js scaffold files — create-next-app WILL NOT overwrite them -->
<!-- docs/ contains: 00-PRD.md, 01-08 real markdown files, CLAUDE.md, DESIGN.md -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 14 App Router project</name>
  <files>package.json, tsconfig.json, tailwind.config.ts, next.config.ts, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx, .gitignore, README.md</files>
  <action>
Run the locked create-next-app command in the existing project root. The `--yes` flag accepts all conflict prompts (for .gitignore, README.md). CLAUDE.md, DESIGN.md, and docs/ are NOT Next.js scaffold files and will not be overwritten.

After scaffolding completes, immediately verify that CLAUDE.md, DESIGN.md, and docs/ are still present. If any are missing, they must be restored from git before proceeding.

```bash
cd "/c/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/crowe-ai-onboarding"
NODE_TLS_REJECT_UNAUTHORIZED=0 npx create-next-app@latest . \
  --typescript --tailwind --eslint --app --src-dir \
  --import-alias "@/*" --yes
```

After the command completes:
- Confirm `package.json` exists
- Confirm `tsconfig.json` exists
- Confirm `src/app/globals.css` exists
- Confirm `CLAUDE.md` still exists (not overwritten)
- Confirm `docs/` directory still present with 01-08 files
  </action>
  <verify>
    <automated>cd "/c/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/crowe-ai-onboarding" && test -f package.json && test -f tsconfig.json && test -f src/app/globals.css && test -f CLAUDE.md && ls docs/01-*.md docs/08-*.md && echo "scaffold OK"</automated>
  </verify>
  <done>package.json, tsconfig.json, src/app/globals.css, CLAUDE.md, and docs/01-08 all exist. npm run dev -- --help exits without error.</done>
</task>

<task type="auto">
  <name>Task 2: Install project dependencies and init shadcn</name>
  <files>package.json, package-lock.json, node_modules/, components.json, src/app/globals.css</files>
  <action>
Install the two production dependencies not included by create-next-app, then initialize shadcn/ui with theme-only config (no individual components).

Step 1 — Install Neon + OpenAI:
```bash
cd "/c/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/crowe-ai-onboarding"
NODE_TLS_REJECT_UNAUTHORIZED=0 npm install @neondatabase/serverless openai
```

Step 2 — Init shadcn (NODE_TLS_REJECT_UNAUTHORIZED=0 is required on Crowe network for HTTPS registry calls):
```bash
NODE_TLS_REJECT_UNAUTHORIZED=0 npx shadcn@latest init --yes
```

If shadcn init prompts interactively (--yes may not suppress all prompts): choose style "default", choose "yes" to CSS variables, choose no to adding components. The shadcn init writes its default CSS vars to globals.css — this is correct behavior. The brand-specific HSL overrides are applied in Plan 02 (not here).

Do NOT add any shadcn components (Button, Card, etc.) — that is Phase 5 work. shadcn init only.

After init:
- Confirm `components.json` exists at project root
- Confirm `src/app/globals.css` contains `--background` and `--primary` CSS variables (written by shadcn)
- Confirm `package.json` lists `@neondatabase/serverless` and `openai` in dependencies
  </action>
  <verify>
    <automated>cd "/c/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/crowe-ai-onboarding" && test -f components.json && grep -q "@neondatabase/serverless" package.json && grep -q '"openai"' package.json && grep -q "\-\-background" src/app/globals.css && echo "deps and shadcn OK"</automated>
  </verify>
  <done>components.json exists. package.json contains @neondatabase/serverless and openai. globals.css contains shadcn CSS variable block with --background and --primary. npm run build exits with zero TypeScript errors.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `npm run build` exits 0 (zero TypeScript errors, successful compilation)
2. `test -f package.json && test -f tsconfig.json && test -f components.json` — all three exist
3. `grep -q "@neondatabase/serverless" package.json` — Neon driver present
4. `grep -q '"openai"' package.json` — OpenAI SDK present
5. `grep -q "\-\-background" src/app/globals.css` — shadcn CSS vars present
6. CLAUDE.md, DESIGN.md, docs/ all intact (not overwritten by scaffold)
</verification>

<success_criteria>
- Next.js 14 App Router project scaffolded in existing project root
- @neondatabase/serverless and openai installed as dependencies
- shadcn/ui initialized (components.json exists, globals.css has CSS var block)
- npm run build exits 0
- Pre-existing files (CLAUDE.md, DESIGN.md, docs/) preserved
</success_criteria>

<output>
After completion, create `.planning/phases/02-rag-app-scaffold/02-01-SUMMARY.md` covering:
- Exact Next.js version installed (from package.json)
- Whether next.config.ts or next.config.js was generated
- Any unexpected prompts or conflicts during create-next-app or shadcn init
- Confirmation that CLAUDE.md, DESIGN.md, docs/ were preserved
</output>
