---
phase: 01-infrastructure-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .vercel/project.json
autonomous: false
requirements:
  - INFRA-01
must_haves:
  truths:
    - "GitHub repo achyuthrachur/crowe-ai-onboarding exists and is public"
    - "Vercel project crowe-ai-onboarding is linked to the GitHub repo"
    - "Vercel project is under team achyuth-rachurs-projects (orgId team_jTMSsUBJBbOqgNTyjjsr9PY2)"
  artifacts:
    - path: ".vercel/project.json"
      provides: "Vercel project link configuration"
      contains: "team_jTMSsUBJBbOqgNTyjjsr9PY2"
  key_links:
    - from: ".vercel/project.json"
      to: "Vercel project crowe-ai-onboarding"
      via: "orgId field"
      pattern: "team_jTMSsUBJBbOqgNTyjjsr9PY2"
---

<objective>
Initialize the git repository, create the GitHub repo under achyuthrachur, and link the Vercel project to that repo under the achyuth-rachurs-projects team. This is the prerequisite chain for all Neon provisioning and deployment steps.

Purpose: Vercel requires a linked GitHub repo before it can provision databases or run deployments. Getting the team linkage right now prevents pain later.
Output: .vercel/project.json with correct orgId; GitHub repo live at achyuthrachur/crowe-ai-onboarding.
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-setup/01-CONTEXT.md
@.planning/phases/01-infrastructure-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Git init and initial commit</name>
  <files>docs/ (existing)</files>
  <action>
Initialize git in the project root and commit the existing docs/ folder as the first commit.

Run these commands in order from the project root (`/c/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/crowe-ai-onboarding`):

```bash
cd "/c/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/crowe-ai-onboarding"
git init
git add .
git commit -m "chore: initial commit — docs and planning files"
```

Do NOT add NODE_TLS_REJECT_UNAUTHORIZED=0 to any file. Do NOT create a .gitignore yet (Next.js scaffold in Phase 2 will generate it). The docs/ folder is the only existing content — commit it as-is.
  </action>
  <verify>
    <automated>cd "/c/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/crowe-ai-onboarding" && git log --oneline -1</automated>
  </verify>
  <done>git log shows the initial commit; git status shows clean working tree</done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub repo and push</name>
  <files>docs/ (existing — pushed to remote)</files>
  <action>
Create the public GitHub repo achyuthrachur/crowe-ai-onboarding using the gh CLI at its full path, then push the initial commit.

```bash
cd "/c/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/crowe-ai-onboarding"
GH="/c/Users/RachurA/AppData/Local/gh-cli/bin/gh.exe"
"$GH" repo create achyuthrachur/crowe-ai-onboarding \
  --public \
  --description "RAG-powered onboarding assistant for Crowe AI practice" \
  --source=. \
  --remote=origin \
  --push
```

The gh CLI must be invoked at full path — it is NOT on the system PATH. If the command errors with "repository already exists," verify using `"$GH" repo view achyuthrachur/crowe-ai-onboarding` to confirm it is linked to this working directory.
  </action>
  <verify>
    <automated>GH="/c/Users/RachurA/AppData/Local/gh-cli/bin/gh.exe" && "$GH" repo view achyuthrachur/crowe-ai-onboarding --json name,visibility,url | cat</automated>
  </verify>
  <done>gh repo view returns the repo as public at github.com/achyuthrachur/crowe-ai-onboarding; git remote -v shows origin pointing to that URL</done>
</task>

<task type="auto">
  <name>Task 3: Link Vercel project to GitHub repo</name>
  <files>.vercel/project.json</files>
  <action>
Link this directory to the Vercel project crowe-ai-onboarding under the achyuth-rachurs-projects team. The Crowe SSL proxy requires NODE_TLS_REJECT_UNAUTHORIZED=0 as a shell prefix — NOT in any .env file.

```bash
cd "/c/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/crowe-ai-onboarding"
NODE_TLS_REJECT_UNAUTHORIZED=0 vercel link --yes --project crowe-ai-onboarding
```

When prompted to select scope/team: select `achyuth-rachurs-projects` (NOT the personal account). The `--yes` flag confirms the project name but may still prompt for team selection interactively.

If the link fails because the project does not exist yet on Vercel, it will attempt to create it — accept the prompts and ensure `achyuth-rachurs-projects` is selected as the team.

After linking, immediately verify the orgId in .vercel/project.json. If the orgId does NOT start with `team_` (meaning it linked to the personal account instead of the team), run `vercel unlink` and repeat this step, explicitly choosing the team.
  </action>
  <verify>
    <automated>cd "/c/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/crowe-ai-onboarding" && cat .vercel/project.json</automated>
  </verify>
  <done>.vercel/project.json exists and contains orgId: "team_jTMSsUBJBbOqgNTyjjsr9PY2"; projectName is "crowe-ai-onboarding"</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm Vercel is linked to GitHub repo</name>
  <what-built>Git repo initialized, GitHub repo created, Vercel project linked to achyuth-rachurs-projects team</what-built>
  <how-to-verify>
1. Run: `cat "/c/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/crowe-ai-onboarding/.vercel/project.json"` — confirm orgId is `team_jTMSsUBJBbOqgNTyjjsr9PY2`
2. Open Vercel dashboard → project crowe-ai-onboarding → Settings → Git → confirm it is connected to `achyuthrachur/crowe-ai-onboarding` on GitHub
3. If Git integration is not showing: in Vercel dashboard, manually connect the GitHub repo under Settings → Git → Connect Git Repository
  </how-to-verify>
  <resume-signal>Type "approved" when Vercel dashboard shows GitHub repo linked, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `cat .vercel/project.json` shows orgId: "team_jTMSsUBJBbOqgNTyjjsr9PY2"
- `GH="/c/Users/RachurA/AppData/Local/gh-cli/bin/gh.exe" && "$GH" repo view achyuthrachur/crowe-ai-onboarding` returns the repo
- Vercel dashboard Settings → Git confirms the GitHub repo is connected
</verification>

<success_criteria>
- GitHub repo achyuthrachur/crowe-ai-onboarding is public and accessible
- .vercel/project.json exists with orgId team_jTMSsUBJBbOqgNTyjjsr9PY2
- Vercel dashboard shows GitHub integration connected to achyuthrachur/crowe-ai-onboarding
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-setup/01-01-SUMMARY.md` following the summary template.
</output>
